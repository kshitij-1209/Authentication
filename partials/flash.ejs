<% if(success && success.length) { %>
    <div class="alert-cont alert-success">
        <p><%= success %></p>
    </div>
<% } %>

<% if(error && error.length) { %>
    <div class="alert-cont alert-error">
        <p><%= error %></p>
    </div>
<% } %>